<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>个人资料 - 安全版本</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="container">
    <div class="header">
      <a href="/secure" class="back-link">← 返回安全版本</a>
      <h1>个人资料</h1>
      <p class="info">✅ 已启用三层纵深防御</p>
    </div>

    <div class="content">
      <div class="form-section">
        <h2>创建/更新资料</h2>
        <form action="/secure/profile" method="POST">
          <div class="form-group">
            <label for="username">用户名：</label>
            <input type="text" id="username" name="username" required maxlength="30"
                   pattern="[a-zA-Z0-9_]+"
                   title="只能包含字母、数字和下划线">
            <small>只能包含字母、数字和下划线，1-30个字符</small>
          </div>
          <div class="form-group">
            <label for="bio">个人简介：</label>
            <textarea id="bio" name="bio" maxlength="500" rows="3"></textarea>
            <small>最多500个字符</small>
          </div>
          <div class="form-group">
            <label for="website">个人网站：</label>
            <input type="url" id="website" name="website" placeholder="https://example.com">
            <small>请输入有效的URL地址</small>
          </div>
          <button type="submit">保存资料</button>
        </form>
      </div>

      <div class="users-section">
        <h2>用户列表</h2>
        <% if (users && users.length > 0) { %>
          <div class="users">
            <% users.forEach(function(user) { %>
              <div class="user-card">
                <h3><%= user.username %></h3>
                <% if (user.bio) { %>
                  <p class="bio"><%= user.bio %></p>
                <% } %>
                <% if (user.website) { %>
                  <p class="website">网站：<%= user.website %></p>
                <% } %>
                <a href="/secure/user/<%= user.id %>">查看详情</a>
              </div>
            <% }); %>
          </div>
        <% } else { %>
          <p class="empty">暂无用户资料</p>
        <% } %>
      </div>

      <div class="defense-info">
        <h3>防御机制说明</h3>
        <ul>
          <li><strong>用户名验证：</strong>正则表达式白名单，只允许字母、数字、下划线</li>
          <li><strong>简介验证：</strong>长度限制500字符，输出时自动编码</li>
          <li><strong>网站验证：</strong>URL格式验证，防止注入</li>
          <li><strong>XSS过滤：</strong>拦截常见攻击向量</li>
        </ul>
      </div>

      <div class="test-section">
        <h3>XSS测试</h3>
        <p>尝试使用与漏洞版本相同的测试代码，验证防御效果：</p>
        <div class="test-code">
          <code>&lt;img src=x onerror=alert('XSS')&gt;</code>
          <code>&lt;svg onload=alert('XSS')&gt;</code>
          <code>&lt;script&gt;alert(document.cookie)&lt;/script&gt;</code>
        </div>
        <p class="info">预期结果：XSS攻击被拦截或内容被净化</p>
      </div>
    </div>

    <footer>
      <p><a href="/">返回首页</a></p>
    </footer>
  </div>
</body>
</html>
