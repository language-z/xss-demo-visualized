<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>搜索 - 安全版本</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="container">
    <div class="header">
      <a href="/secure" class="back-link">← 返回安全版本</a>
      <h1>搜索</h1>
      <p class="info">✅ 已启用三层纵深防御</p>
    </div>

    <div class="content">
      <div class="search-section">
        <form action="/secure/search" method="GET">
          <div class="form-group">
            <label for="q">搜索关键词：</label>
            <input type="text" id="q" name="q" value="<%= query %>" placeholder="输入搜索内容">
          </div>
          <button type="submit">搜索</button>
        </form>
      </div>

      <% if (query) { %>
        <div class="search-results">
          <h2>搜索结果</h2>
          <p>搜索关键词：<span class="query"><%= query %></span></p>
          <p class="info">如果您没有看到弹窗，说明反射型XSS防御生效！</p>
        </div>
      <% } %>

      <div class="defense-info">
        <h3>防御机制说明</h3>
        <ul>
          <li><strong>XSS过滤：</strong>拦截常见XSS攻击向量</li>
          <li><strong>输出净化：</strong>DOMPurify净化搜索结果</li>
          <li><strong>CSP防护：</strong>限制脚本执行来源</li>
        </ul>
      </div>

      <div class="test-section">
        <h3>XSS测试</h3>
        <p>尝试使用与漏洞版本相同的测试代码，验证防御效果：</p>
        <div class="test-code">
          <code>&lt;script&gt;alert('XSS')&lt;/script&gt;</code>
          <code>&lt;img src=x onerror=alert('XSS')&gt;</code>
          <code>javascript:alert('XSS')</code>
        </div>
        <p>或者直接访问以下URL：</p>
        <code>/secure/search?q=&lt;script&gt;alert('XSS')&lt;/script&gt;</code>
        <p class="info">预期结果：XSS攻击被拦截或内容被净化，不会执行弹窗</p>
      </div>
    </div>

    <footer>
      <p><a href="/">返回首页</a></p>
    </footer>
  </div>
</body>
</html>
